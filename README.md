# donuts-lisp

This project aims to implement donut.c in different lisp dialects.

Each version consists of three files: the donut file with the donut shaped code, a file with the code in a single line and a file with more verbose code to see how the implementation works.

Versions implemented:
1. donut.rkt